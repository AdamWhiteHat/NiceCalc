namespace TestNiceCalc
{
	public class Tests
	{
		[SetUp]
		public void Setup()
		{
		}
		/*
		[Test]
		[TestCase("", "")]
		public void TestEvaluate(string infixNotationString, string expecting)
		{
			BigDecimal result = InfixNotation.Evaluate(infixNotationString);

			Assert.AreEqual(expecting, result);
		}
		*/
	}
}